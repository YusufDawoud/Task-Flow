function Topbar({ title }) {
  return (
    <header className="mb-4 d-flex align-items-center justify-content-between">
      <h1 className="mb-0 fs-4">{title}</h1>
    </header>
  );
}

export default Topbar;
